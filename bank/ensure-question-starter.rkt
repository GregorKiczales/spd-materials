;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-beginner-reader.ss" "lang")((modname ensure-question-starter) (read-case-sensitive #t) (teachpacks ()) (htdp-settings #(#t constructor repeating-decimal #f #t none #f () #f)))
(require spd/tags)

(@assignment bank/htdf-p8)
(@cwl ???)

(@problem 1)
;; Use the How to Design Functions (HtDF) recipe to design a function that
;; consumes a string, and adds "?" to the end unless the string already ends in
;; "?".
;;
;; For this question, assume the string has length > 0. Follow the HtDF recipe
;; and leave behind a commented out version of the stub.

