;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-beginner-reader.ss" "lang")((modname evaluation-prims-starter) (read-case-sensitive #t) (teachpacks ()) (htdp-settings #(#t constructor repeating-decimal #f #t none #f () #f)))
(require spd/tags)

(@assignment bank/primitive-evaluation)
(@cwl ???)

(@problem 1)
;;
;; Write the step-by-step evaluation of the expression below.
;;
;; Be sure not to comment out or delete the original expression, show every
;; intermediate evaluation step, and the final result.  The steps MUST NOT
;; be in comments.
;;
;; Stepping questions like this one have ONLY ONE CORRECT ANSWER.  The goal
;; of this problem is to assess whether you have learned the exact BSL step
;; by step evaluation rules; not whether you can figure out the final result
;; of an expression.
;;
(+ (* 2 3) (/ 8 2))








(@problem 2)
;;
;; Write the step-by-step evaluation of the expression below.
;;
;; Be sure not to comment out or delete the original expression, show every
;; intermediate evaluation step, and the final result.  The steps MUST NOT
;; be in comments.
;;
(* (string-length "foo") 2)

